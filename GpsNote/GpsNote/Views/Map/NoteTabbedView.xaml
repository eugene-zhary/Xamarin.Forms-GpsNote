<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:prism="http://prismlibrary.com"
            prism:ViewModelLocator.AutowireViewModel="True"
            x:Class="GpsNote.Views.NoteTabbedView" Title="{Binding Title}"
            xmlns:local="clr-namespace:GpsNote.Views" 
            BarBackgroundColor="DimGray" BarTextColor="White">
    <TabbedPage.ToolbarItems>
        <ToolbarItem Text="Log out" Command="{Binding LogoutCommand}"/>
    </TabbedPage.ToolbarItems>

    <local:MapPage BindingContext="{Binding PinsViewModel}"/>
    <local:PinsPage BindingContext="{Binding PinsViewModel}"/>
</TabbedPage>