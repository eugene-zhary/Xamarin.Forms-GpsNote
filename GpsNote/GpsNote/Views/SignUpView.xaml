<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GpsNote.Views.SignUpView"
             Title="{Binding Title}" BackgroundColor="LightGray">
    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="Gray"/>
            <Setter Property="TextColor" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
    </ContentPage.Resources>
    <Frame VerticalOptions="Center" CornerRadius="15" Margin="15,0">
        <Grid Padding="0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Entry Grid.Row="0" Text="{Binding Name}" Placeholder="name"/>
            <Entry Grid.Row="1" Text="{Binding Email}" Placeholder="email"/>
            <Entry Grid.Row="2" Text="{Binding Password}" Placeholder="password" Margin="0,0,0,30"/>

            <Button Grid.Row="3" Text="Submit" Margin="0,0,10,0" Command="{Binding CreateCommand}"/>
        </Grid>
    </Frame>
</ContentPage>